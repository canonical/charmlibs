[project]
name = "slo-test-provider"
version = "0.1.0"
description = "Test charm for SLO provider"
dependencies = [
    "ops>=2.9",
    "pydantic>=2.0",
    "pyyaml",
]

[project.optional-dependencies]
dev = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
