[project]
name = "slo-test-requirer"
version = "0.1.0"
description = "Test charm for SLO requirer"
requires-python = ">=3.10"
dependencies = [
    "ops>=2.9",
    "charmlibs-interfaces-slo",
]

[tool.uv.sources]
charmlibs-interfaces-slo = { path = "library", editable = true }
