# charmlibs.grub

The `grub` library.

To install, add `charmlibs-grub` to your Python dependencies. Then in your Python code, import as:

```py
from charmlibs import grub
```

See the [reference documentation](https://documentation.ubuntu.com/charmlibs/reference/charmlibs/grub) for more.
